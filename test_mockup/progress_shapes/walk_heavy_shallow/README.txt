This synthetic fixture stresses the walking phase.
It contains many small, unique files under a single shallow directory
so filesystem traversal and per-file bookkeeping dominate, while
grouping finds almost no duplicates.

