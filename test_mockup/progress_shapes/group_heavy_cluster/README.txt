This synthetic fixture stresses the grouping phase.
It contains many near-identical folder copies so the similarity engine
must evaluate a dense set of candidate pairs even though the raw walk
work is relatively small.

